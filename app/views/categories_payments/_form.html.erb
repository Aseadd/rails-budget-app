<%= form_with(model: categories_payment) do |form| %>
  <% if categories_payment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(categories_payment.errors.count, "error") %> prohibited this categories_payment from being saved:</h2>

      <ul>
        <% categories_payment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Categories, style: "display: block" %>
    <%= form.text_field :Categories %>
  </div>

  <div>
    <%= form.label :references, style: "display: block" %>
    <%= form.text_field :references %>
  </div>

  <div>
    <%= form.label :Payments_id, style: "display: block" %>
    <%= form.text_field :Payments_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
